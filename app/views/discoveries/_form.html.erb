<%# <%= form_with url: "restaurants", method: :post, local: true do |f| %>
<%= simple_form_for(@discovery) url: "restaurants", method: :post, local: true do |f| %>
  <%= f.hidden_field :activity %>
  <%= f.submit %>
<% end %>
